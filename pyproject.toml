[project]
name = "01-home-monitor"
version = "0.1.0"
description = "includes all dependencies required for the project"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "Pillow>=9.0.0",
    "RPi.GPIO>=0.7.0",
    "spidev>=3.5",
    "gpiozero>=1.6.2",
    "lgpio>=0.2.2",
]
